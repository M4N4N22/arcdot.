# Agent protocol (pointer)

Customer-facing documentation lives in the app:

- Hub: `/docs`
- Quickstart: `/docs/quickstart`
- Agents / sellers / discovery / payment / API / CLI under `/docs/*`

This file is for contributors. Do not duplicate product docs here.

**Locks:** Arc Mainnet 5042 · native USDC 18 decimals · 0.01 = `1e16` wei · EIP-191 unlock · settlement via `depositPayment` (not 6-decimal ERC-20).
