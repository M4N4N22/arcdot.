{
  "name": "arcdot-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:arc": "hardhat run scripts/deploy.ts --network arcMainnet",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network arcTestnet",
    "deposit:arc": "hardhat run scripts/deposit.ts --network arcMainnet",
    "deposit:testnet": "hardhat run scripts/deposit.ts --network arcTestnet",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@types/node": "^20.19.0",
    "dotenv": "^18.0.1",
    "hardhat": "^2.26.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.0"
  }
}
